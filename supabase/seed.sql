-- Optional SQL seed for a live Supabase project.
-- The app also ships a richer local demo seed in src/lib/demo/seed.ts.

-- After creating an org + profile via auth, insert sample rows scoped to that organization_id.
