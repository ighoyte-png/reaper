# Static marketing site for reaperpm.com

Deploy the contents of this folder to the apex domain (e.g. Cloudflare Pages, Netlify, S3+CloudFront).

```bash
# From repo root — preview locally
npx --yes serve website
```

Point **app.reaperpm.com** at the Next.js app; keep **reaperpm.com** on this static site.

Primary CTA targets: `https://app.reaperpm.com`
